import 'package:flutter/material.dart';

class AppColors {
  // Couleur principale avec opacité 0.8 (RGBA(8, 27, 28, 0.8))
  static const Color primaryColor = Color.fromRGBO(8, 27, 28, 0.8);
  
  // Variantes de la couleur principale
  static const Color primaryColorDark = Color.fromRGBO(5, 18, 19, 1);
  static const Color primaryColorLight = Color.fromRGBO(12, 36, 38, 1);
  
  // Couleurs complémentaires
  static const Color accentColor = Color(0xFF4CAF50);
  static const Color textOnPrimary = Colors.white;
  static const Color background = Colors.white;
}